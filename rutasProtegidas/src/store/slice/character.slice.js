import { createSlice } from "@reduxjs/toolkit";

export const characterSlice = createSlice({
    name:'character',
    initialState:'pikachu',
    reducers:{

    }
})
export default characterSlice.reducer